<div>
	<h4>copy to command to download directly(脚本会自动识别平台)</h4>
	<code>
		curl -s http://{{.Server}}/dlscript/{{.Project}} | bash
	</code>
	<h4>download through url(need to specify os and arch), for example below</h4>
	<code>
		curl -s "http://{{.Server}}/dl/latest?p={{.Project}}&os=windows&arch=amd64"
	</code>
</div>
<div>
	<h3>downloads: <i>{{.Project}}</i></h3>
</div>
<div>
	<ul>
	{{range .Files}}
		 <li><a href={{.}}>{{.}}</a></li>
	{{end}}
	</ul>
</div>

